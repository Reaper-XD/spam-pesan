# Spam Pesan

<pre><code>
$pkg update && pkg upgrade
$pkg install python
$pkg install nano
$pkg install figlet
$pkg install git
$pip install requests
$git clone https://githun.com/GRCR4K3R/spam-pesan
$cd spam-pesan
$python spam.py
</code></pre>
